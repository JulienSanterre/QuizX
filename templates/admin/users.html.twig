{% extends 'base.html.twig' %}

{% block title %}Utilisateurs - {{ parent() }} {% endblock %}

{% block body %}
<div class="row">
    <div class="col-12">
    <table class="table table-dark">
        <thead>
            <tr>
            <th scope="col">Nom</th>
            <th scope="col">Actions</th>
            </tr>
        </thead>
        <tbody>
            {% for user in users %}
            <tr>
            <th scope="row">
                <a href="{{ path('admin_show_users_single', {'id': user.id}) }}">
                    {{ user.username }}
                </a>
            </th>
            <td>
                <a href="{{ path('admin_edit_users', {'id': user.id}) }}">
                    <i class="fa fa-pencil-square-o" aria-hidden="true"></i>
                </a>
            </td>
            </tr>
            {% endfor %}
        </tbody>
        </table>
    </div>
    <a href="{{ path('home') }}" class="previous-link col-12 text-center">Retour a l'acceuil</a>
</div>
{% endblock %}